import 'package:flutter/material.dart';

class HosieryScreen extends StatelessWidget {
  const HosieryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hosiery'),
      ),
    );
  }
}
